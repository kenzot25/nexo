## nexo
- **nexo** (`.claude/skills/nexo/SKILL.md`) - knowledge graph explorer. Trigger: `/nexo` or `/nexo`
When the user asks about project architecture, cross-file relationships, or uses `/nexo` or `/nexo`, invoke the Skill tool with `skill: "nexo"` immediately.
